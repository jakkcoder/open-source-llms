[project]
name = "local-airflow"
version = "0.1.0"
description = "Minimal local Apache Airflow (standalone) with example dummy DAGs."
requires-python = ">=3.11,<3.13"
dependencies = [
    "apache-airflow==2.10.5",
]
